#include<stdio.h>  
#include<string.h>  
  
int main(){  
    char temp[128];  
    int len;  
    while(fgets(temp,128,stdin)!=NULL){  
        len=strlen(temp);  
        len--;  
    }  
    printf("There are %d characters\n",len);  
    return 0;  
}
